{
  "name": "bindar-scripts",
  "version": "1.0.0",
  "private": true,
  "description": "Server-side scripts (seed, sync). Runs locally, NEVER bundled in the mobile app.",
  "type": "module",
  "scripts": {
    "seed:gundam": "tsx seed-gundam.ts",
    "seed:onepiece": "tsx seed-onepiece.ts",
    "smoke-test": "tsx smoke-test.ts",
    "smoke-test-trades": "tsx smoke-test-trades.ts"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "cheerio": "^1.0.0",
    "dotenv": "^16.4.5",
    "sharp": "^0.35.2"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsx": "^4.19.0",
    "typescript": "~5.3.3"
  }
}
